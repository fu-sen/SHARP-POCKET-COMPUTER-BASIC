LCOPY
LC.
? マニュアル
-----------------------------------------------------------
[機能]
プログラムの一部行を複写する。

[書式]
LCOPY コピー元開始行番号,コピー元終了行番号,コピー先開始行番号

[説明]
PRO モードでマニュアル操作時のみ有効。
GOTO・GOSUB・RETURN などの行番号がある場合、その値は変更されないため、手作業で変更する必要がある。
パスワードを設定している場合は無視される。

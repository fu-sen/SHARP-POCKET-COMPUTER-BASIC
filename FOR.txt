FOR TO STEP
F. TO STE.
E200 プログラム
-----------------------------------------------------------
[機能]
NEXT のところまで条件を満たすまでくり返し実行する。

[書式]
FOR 数値変数=初期値 TO 最終値 [STEP 増値]

[説明]
各値は以下の値で設定できる
-9.999999999E99～9.99999999E99
最終値以上の値になったところで、NEXT から後の命令が実行される
STEP 0 とした場合は無限ループになる

